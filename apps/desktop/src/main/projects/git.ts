/**
 * Git modes, worktrees, status and diff. **Filled by P7** (plan §9).
 *
 * Three modes: `none` (the project directory, git untouched), `checkout` (the
 * project directory on its current branch) and `worktree` (a new branch under
 * the worktree root, `~/.hardcore/worktrees/<project>/<name>` by default, with
 * the `hardcore/` branch prefix). Also the review tab's status/diff reads and
 * its `Commit or push` action, and `Create pull request` via `gh` when present.
 */
export {};

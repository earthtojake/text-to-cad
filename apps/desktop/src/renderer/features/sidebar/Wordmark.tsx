/**
 * The wordmark: HARDCORE in JetBrains Mono ExtraBold Italic, the ink alone —
 * no offset copy, no hover. `.brand-wordmark` in globals.css sets the face.
 */
export function Wordmark() {
  return (
    <span aria-label="Hardcore" className="brand-wordmark app-no-drag" role="img">
      HARDCORE
    </span>
  );
}

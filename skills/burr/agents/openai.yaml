interface:
  display_name: "Burr"
  short_description: "Generate CAD with Burr design-rule checks in the loop."
  default_prompt: "Use $burr to create or modify STEP-first parametric CAD source, emit Burr design metadata, run an explicit rulepack, repair failed or incomplete source-level claims from Burr receipts, and provide checked outputs with honest caveats."
